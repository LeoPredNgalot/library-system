


// FRONT END FOLDERS/FILES


1. `public folder` - contains css and JavaScript files
    the JavaScripts are for each webpage.

1.2 `public folder` also contains a `js folder` with `confirmModal.js`,
     a JavaScript file related sa FE stuff. A copy paste from cgpt.
     E ignore lang sah ni.

     What matters are the javascript that are directly under sa `public`
     folder. 
                                
2. `views folder` - contains ejs files. What is ejs? 
    an ejs file is an HTML file that can be embedded with JavaScript code
    with additional syntaxes like: <% %>

    So, HTML + javascript = `.ejs`

3. UI folder contains JavaScript code generated by cgpt   
   for pop-up sa  front-end.

   Admittedly, naay mga files nga wah na place ug tarung.
   like aning files sa modal, pero no worries dali ra sila
   sabtun. E ignore lang sa.


4. What you should really care:

    1. `views folder`
    2. `public folder` 



5. BE - backend
   FE - front-end


 
